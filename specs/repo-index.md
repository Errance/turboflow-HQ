# Turboflow Repo Index

This document summarizes repository discovery for the `turboflow-xyz` GitHub organization. The structured source of truth is `ops/repo-index.json`.

## Current Sync

- Source org: `https://github.com/turboflow-xyz`
- Sync method: GitHub public API
- Public repos visible: `0`
- Private repo enumeration: blocked by local `gh` auth
- Local `turboflow-xyz` checkouts found: `0`

## Indexed Repos

No product repositories are visible to this session yet.

## Next Sync Requirement

Run `gh auth login` or otherwise restore GitHub CLI authentication, then rerun Magic Flow repo sync so private organization repositories can be enumerated, cloned, and registered in `ops/repos.json`.
